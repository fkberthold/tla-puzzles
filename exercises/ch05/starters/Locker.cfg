\* SKELETON .cfg for exercise 3.
\* Add the CONSTANT line yourself. Run it once each way.
\* As it stands the run reports CONFIG_ERROR, because `Unclaimed` is declared
\* in the module and assigned nowhere. That is your first observation.
SPECIFICATION Spec
INVARIANT FreeIffSentinel
