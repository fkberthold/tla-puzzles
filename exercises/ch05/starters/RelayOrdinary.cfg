\* FAIL run. The same SYMMETRY line over ordinary integers.
\* Integers support arithmetic and ordering, so swapping 1 for 2 is not a
\* relabelling that leaves the spec alone. TLC refuses the request.
SPECIFICATION Spec
INVARIANT TouchedAreRunners
INVARIANT CarrierIsRunner
CONSTANT Runners = {1, 2, 3}
SYMMETRY Perms
