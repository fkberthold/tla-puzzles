\* PASS run. Three model values, declared as a set without quotes,
\* then handed to TLC as a symmetry set through the SYMMETRY line.
SPECIFICATION Spec
INVARIANT TouchedAreRunners
INVARIANT CarrierIsRunner
CONSTANT Runners = {r1, r2, r3}
SYMMETRY Perms
