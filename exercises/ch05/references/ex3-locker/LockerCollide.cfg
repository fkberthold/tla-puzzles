\* FAIL run. An ordinary integer sentinel that is also a real slot id.
\* Claiming slot 2 leaves `holder` equal to the sentinel, so the spec now
\* believes the locker is free and claimed at the same time.
SPECIFICATION Spec
INVARIANT FreeIffSentinel
CONSTANT Unclaimed = 2
