# Malting Floor Model - Solution Report

## Spec and Configuration

**Malting.tla** module contains:
- Three actions: Turn(p) increments modification if p on floor and mod < UpperMark; Kiln(p) sends piece to malt (if LowerMark ≤ mod < UpperMark) or loss; ThrowOut(p) sends piece to loss.
- Seven requirements as temporal formulas over Observe record.
- Spec with weak fairness on (Kiln(p) ∨ ThrowOut(p)) for each piece to enforce requirement 7.

**Malting.cfg** instance:
```
Pieces = {p1, p2, p3}
Maltsters = {m1, m2}
LowerMark = 1
UpperMark = 3
NoCount = NoCount
CHECK_DEADLOCK FALSE
```

## TLC Result
- **Exit code**: 0 (success)
- **Distinct states**: 216 (matches expected minimum)
- **All properties satisfied**

## Trace Pair Verification

**Pair 1 (opening)**: Requirement1 rejects forbidden (starts with mod 1 instead of 0). Allowed trace constructible by model.

**Pair 2 (count/mark)**: Requirement2 (invariant) rejects forbidden (off-floor piece has mod 0 instead of NoCount). Allowed trace valid.

**Pair 3 (one pair)**: Requirement3 rejects forbidden (three pieces change simultaneously). Allowed trace valid (single-piece transitions).

**Pair 4 (turning)**: Requirement4 rejects forbidden (mod jumps 0→2). Allowed trace valid (each turn adds exactly 1).

**Pair 5 (ready malt)**: Requirement5 rejects forbidden (piece becomes malt from mod 0, not ready). Allowed trace valid (mod 1 is in [1,3)).

**Pair 6 (final)**: Requirement6 rejects forbidden (piece returns to floor). Allowed trace valid (off-floor state is final).

**Pair 7 (cleared)**: Requirement7 rejects forbidden (piece stays on floor forever). Allowed trace valid (all pieces eventually leave via fairness).

## Development Notes
- One attempt: model was green on first run after fixing Requirement5 to check only transitions from non-malt to malt.
- TLC caught no bugs in the spec; initial formulation handled all traces correctly after fix.
- No revisions mid-way; all seven requirements worked as written.

## Requirements Carried
All seven requirements successfully encoded and verified. No requirements dropped.

## Clarifications
Problem statement is clear. Excellent specification of the malting floor system with precise rules and trace pairs covering each requirement independently.

## Mechanism Recognition
Recognized immediately as **floor malting** (grain processing on a bed). Read the introductory narrative and recognized the process: steeping, sprouting (turning), and drying phases. This is a well-known historical malting method. The model captures the turning phase where grain lies on a stone floor.
