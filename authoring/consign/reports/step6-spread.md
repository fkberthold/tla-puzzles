# Step 6 — the panel, and the spread read on the argument

## The panel

| seat | model | attempts to clean | representation |
|---|---|---|---|
| p1 | claude-fable-5 | 1 | the book directly: one variable, `Items -> Standings` |
| p2 | claude-opus-5 | 1 | four sets (`onFloor`, `backHome`, `owed`, `paidOut`), `Observe` a derived view |
| p3 | claude-haiku-4-5 | 4 | `standing` plus an internal `owed` ledger kept out of `Observe` |

One instance each, run 2026-08-08, isolated panel directories holding the seven
learner files. Reports in the panel directories and the session transcript.

## Ruling: GREEN, and the step-5 prediction fails in the right direction

Step 5 predicted low representation spread, cause named as the one-field
interface. The machine panel refutes it: three families produced three
structurally different states, including the declared ledger fork taken (p3)
and a fully derived view (p2). Under rule 2's column-A reading, solvers with
genuinely different representations is the measurable proxy for real modeling
choice. Conclusions converged (all five obligations rendered and green at the
same 608-book space), representations did not. Target achieved.

## Instruments, split per rule 1

- **p2** seeded seven single-defect bugs each against only its targeted
  obligation, replayed all ten oracle traces (five admitted, five refused),
  ran nine reachability probes, and predicted four state counts in closed form
  before running them (all four exact). It also named `Observe` as a
  refinement mapping unprompted and turned that recognition into a
  measurement: its B6 spec holds an item on the floor and in a debt at once,
  keeps a lawful book face throughout, and passes all five obligations.
- **p1** built five mutations and four witness probes, all behaving, with the
  cap and till counterexamples matching the shipped violating traces state for
  state.
- **p3** verified the standard set and stopped; its obligation renderings lean
  on invariants where step form is needed, which is the same shape-divergence
  the pilot's mixed panel caught (one critic in six, rc=12-shaped).

## Findings carried forward

1. **The B6 boundary** (p2): view-level obligations cannot constrain concrete
   incoherence that never surfaces in the book. By §3.3's design the grade
   reads the interface, and this is the measured edge of that design. Goes to
   the grading-engine record beside tla-29m4's freeze lattice, as the same
   boundary seen from the inside.
2. **The kind trap is real at solve time** (p3): a solver who reaches for
   INVARIANT five times passes obligations 1-3 and never checks 4 or 5. The
   statement's kind question is carrying real weight, which is what the B cell
   argument predicted and this A problem confirms from the other side.
3. The recall probes: no published-problem recognition anywhere; mechanism
   namings (resource lifecycle + batched settlement; refinement mapping) are
   generic idioms, consistent with the screens.

## Rule 6

p3 took four attempts (cfg and INSTANCE plumbing), so the every-solver-first-try
flag does not fire. Telemetry: the two strong seats were first-try clean and
p2's route matched the step-5 estimate. Same easy-end note as buyclub, carried
to §7.5.

## Verdict

**Step 6 closes GREEN.** Statement stands as shipped. Step 7 next.
