# Step 6 — the panel, and the spread read on the argument

## The panel

| seat | model | properties | attempts | instruments |
|---|---|---|---|---|
| p1 | claude-fable-5 | 9 | 1 | 162-run trace-property matrix, both wrong-kind demonstrations, the T4A maximal-reading caveat argued correctly |
| p2 | claude-opus-5 | 10 | 1 | ten single-target mutants, independence runs (two redundancies found and kept with reasons), the subscript trap sprung on purpose, four reachability probes, two over-constraint controls |
| p3 | claude-haiku-4-5 | 7 | 1 | green run plus per-pair catch claims, thinner instruments |

One instance each, 2026-08-08, isolated directories holding the eleven learner
files.

## Verdict: GREEN, under the B-shape reading

Rule 2 for spec-in-hand shapes: the pairs are the oracle, so property-content
convergence is the expected output and not a leak. It converged (all three
map the nine pairs to properties; p3's seven collapse two distinctions). The
discrimination lives exactly where the cell's design says it should: in the
KIND decisions and the instruments under them. p1 and p2 both demonstrated the
initial-state-versus-invariant trap in both directions; p2 measured that a
mis-subscripted step rule goes green with no warning against the very mutant
it was written for, which is the statement's central hazard performed live.

The ungraded question the step-5 report told us to ask out loud got asked and
answered: p1 named seven rules its properties cannot carry with the reason
per rule; p2 produced the full taxonomy (permissions unprovable in principle,
absent obligations carried only by restraint plus an over-constraint control,
ground truth outside the interface, idempotence carried by the set shape,
the invisible re-credit guard, envelope granularity degrading to operator
granularity). That taxonomy is the modeling judgment the B cell exists to
teach, produced blind by two families.

## Findings carried forward

1. **Two reference-adjacent redundancies measured** (p2): mutuality and
   credit-monotonicity follow from starts-clean plus the one-credit step
   shape. Matches step 2's finding that CreditIsPermanent has no independent
   arrow. The grader's found/established split should not punish a learner
   who ships eight properties with the coupling argued.
2. **The maximal-trace reading convention matters** (p1's T4A caveat and
   p2's P4A flag agree from opposite directions): only the pair the traces
   mark "nothing ever changes" is a terminal behavior; every other run is a
   prefix. The grader adopts that convention explicitly.
3. Recall probes: p1 named the actual domain (QSL bureaus) from the
   directory name and prose, with no effect on its property set; p2 named
   the CRDT/CALM family and used it soundly (the monotonicity argument that
   licenses CreditIsEarned as an invariant). No published-problem stops.

## Rule 6

All three first-try green on the spec, which for this shape means little
(a green property set is where the worthless ones hide, as p2 said in its own
report) and the flag reads against instruments instead: two of three built
real ones. Recorded, not fired.

**Step 6 closes GREEN.** Statement stands. Step 7 next.
