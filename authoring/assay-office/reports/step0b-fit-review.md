Filed into the record by central after the harness refused the reviewer's own write.

# Fit review, the assay office, rung 4 of batch 2

Opus reviewer, read-only, 2026-09-05, against `authoring/assay-office/DESCRIPTION.md` at `8c32a20`, bead `tla-h2cg.10`. Filed by central because the harness refused the reviewer's own write.

**SEND BACK.** One blocking gap, five minor defects. The domain, the weight, the property set and the fairness call are all right, so this isn't a trim and it isn't a domain change. Every repair is a sentence or two.

## The defects, dearest first

**D1, blocking. Nothing in the hand-off keeps the finding and the act in separate steps.** `DESCRIPTION.md:199-204`, items 5 and 8.

Section 3 licenses fusing the test and the strike, and calls the fusion "close to observationally vacuous". That's right for that pair. It never rules out fusing the test and the deface, and that pair is fatal. A reference where a substandard test defaces in the same motion makes item 3 true with the fairness conjunct removed. Kind 3 is the rung's single new high, and it becomes decoration. The argument against it is at `:350-354`, in section 6, and section 6 never reaches the author (`:7-9`).

Nothing downstream catches it either. `harness/vacuity.sh:59-114` probes five classes (empty state space, empty obligation, dead actions, an unsatisfiable `Spec`, a frozen observation) and none of them is "the liveness holds with the fairness dropped".

Section 2 at `:107-108` does say item 3 is true only because `Spec` carries the conjunct, so the constraint is in the hand-off. It sits there as an aside about a property rather than as a fact about the system, and section 3 pulls the other way. Add one sentence to Rule 4, stated as a system fact so it closes no fork:

Writing the finding down and acting on it are two motions. A ware can sit with a substandard finding against it and its body still whole, and the office can leave it there a while.

Then bound section 3's fusion note to the test and the strike.

**D2, minor. The two-officer justification doesn't hold.** `:224-225`, item 14.

No `Observe` field names an officer (`:162-166`) and officers are interchangeable (`:29-30`), so a one-officer run and a two-officer run produce the same observations. The fairness question is the same at one officer, since the disjunction failure mode needs no colleague. "Lets one officer sit on a substandard ware while a colleague works" isn't an observation this interface can make. The honest reason for two is the rung: step sources 1 is several actors of one kind, read from the statement's parties list. Say that instead.

**D3, minor. The state estimate's factor of four doesn't follow from the shape it names, and one common shape breaks the bound.** `:232-234`, item 14.

A `while TRUE` loop with one label each holds `pc` constant, so the factor is 1 and the count is 125, not 500. The over-estimate runs the safe way. What doesn't: an officer process carrying a chosen-ware local multiplies by 4 per officer, which is 2,000 at three wares and two officers, over the 1,000 that state space 0 allows. Say 125 at bare actions, then tell the author to keep the officer process to one label with no per-officer local, or to record the measured count and re-place the dimension.

**D4, minor. A citation is off by fourteen lines.** `:341`, item 19.

It cites `V2-PLAN.md:335` for "a named party under `WF` places step sources at 2". The pin is at `V2-PLAN.md:321`. Line 335 is the form rubric's level 1. The screener's report carries the same wrong number at `reports/step0-screens.md:75`.

**D5, minor. Section 6 item 2's reason for cutting the give-back isn't the reason.** `:314-316`, item 6.

A give-back needs no maker. An officer can put a finished ware in an outward tray and step sources stays at 1. What an exit really costs is a property. Once wares can leave, "only a struck or defaced ware leaves" has to be graded, and that's a fifth cfg line at a rung capped at four. Item 11 at `:342-344` already carries the other half, that the ware's fate after the office is outside the system.

**D6, minor. The `Init` hole isn't logged for the variant pass, and it's the same class as the one that is.** `:192-197` against `:290-295`, item 8.

An `Init` that starts a ware already tested is green under all three items and the type invariant: item 1 is vacuous on an unmarked undefaced ware, item 2 holds from that state on, item 3 has no obligation without a substandard finding. That's a mutant nobody catches, exactly like the fusion mutant section 5 logs. Add it to the same note so the pass has both.

## The two calls central asked about

**Cutting the take-up and the give-back is right.** With custody, defacing is guarded by holding. Test the ware, find it substandard, put it down, and every officer's defacing step is disabled. Weak fairness over an action disabled forever obliges nothing, so item 3 is false in a reachable state. The description has that at `:305-312` and it holds up. What it calls "machinery this rung has no room for" reads as a cfg-line cost and isn't one, since a guard on the putting-down costs no property. The real cost is that it makes the fairness argument the hard thing at the rung where fairness is already the new thing. Same call, better reason. See D5 for the give-back half.

**Naming the residual `Init` hole rather than grading it is right.** A fourth item makes five cfg lines, which takes property count from 1 to 2. That's a second new high on top of kind 3, and clause (b') allows one. So the alternative isn't available at this rung, and there's no free repair. An opening condition can't fold into item 1, because no `Observe` field says whether the story has started, and it can't ride the type invariant, which is the mistake `:285-288` correctly refuses. Keep the call, add D6.

## The checklist

1. Fields state-dependent. PASS. `finding`, `marked`, `defaced`, three per-ware facts about now (`:138-152`).
2. Sufficiency at the obligation level. D1, D6. All six rows at `:180-187` re-derived and right.
3. Representation neutrality. PASS. Both sides of every fork at `:248-252` compute all three fields.
4. Section 3.2. PASS. One narrowing, mark and defacing never one three-valued stage, declared at `:265-268` with its cost.
5. Self-contained under caveat 2. D1. One near miss: `:53-55` settles that there's no true fineness behind the finding, obliquely. It holds.
6. The declared ambiguities. D5. The other twelve hold up.
7. The shape alibi. PASS. `Wares` a constant and `Observe` total over it does make an arriving ware unrepresentable (`:182`).
8. Never-open cases. D6. Nine re-derived. The no-both derivation at `:95`, the strike-a-defaced-ware case at `:157-159` and the quiescence note at `:236-241` all hold.
9. Step kinds defined observationally. PASS.
10. Permission-shaped rules. PASS. Rule 5 is graded by item 3 being the only liveness property, and `:186` says so.
11. Count. PASS. Four cfg lines: two `INVARIANT` (type plus item 1), two `PROPERTY` (items 2 and 3). `:121-123` settles whose the type invariant is.
12. Kinds match. PASS. One state invariant, one action property, one leads-to. The fairness sits on the defacing step, per officer and per ware (`:107-116`). Declining the coarser form is right.
13. Parties. PASS. One kind, every step theirs (`:28-33`). No clock, no unassigned step. Wares lodged at the opening (`:39-44`). Step sources 1. See D2.
14. Bounds. D3. Type-space arithmetic checks: 12 cubed is 1,728, item 1 cuts each ware to 5, 5 cubed is 125.
15. Section 3.9. PASS, with one thing central needs to know. Item 3's violating behaviour is a lasso, not a finite trace, and `:128-131` says so in the right words. This is the first delivered problem to need exit 13 from the verdict channel for a liveness violation. `harness/fixtures/verdict/LivenessViolation.tla` covers the row, but no problem has driven it.
16. Kind labels. PASS. `:118-119` fixes both subscripts on the whole of `Observe`. Item 3 carries no subscript and the description could say so in a clause.
17. Shape B rendering is real work. PASS. `:258-263` says the spec's variables must not be the field names. Item 1 can't be pattern-matched off a field, and item 3 needs the learner to open `Spec`.
18. Section 3.10. PASS, with the same caveat rung 1 earned. Strip the words and the mechanism is a per-entity lifecycle field with a compelled terminal act. At kind 3 "the failed items must eventually get processed" is a shape a working programmer holds. Tolerable. Don't reuse this domain at shape A without re-running 3.10.
19. Prose. D4. Zero em dashes, zero semicolons.

## Notes for the reference author's brief

- Tell the author the fairness form before they start. `:107-116` has it, and the screener called it the most likely way this rung ships wrong.
- Quiescence needs `CHECK_DEADLOCK FALSE` and not an invented stuttering action (`:236-241`). It doesn't count against the property total.
- The deadlock arrives before every at-standard ware is struck, not only when all wares are dealt with, since Rule 5 leaves striking optional.
- Nobody has run the instance. The 125 is arithmetic.
- The preamble at `:11-14` carries the load vector. Leave it behind when pasting.
