\* A legal constants fragment: CONSTANT declarations and nothing else.
\* Spliced verbatim into the .cfg refinement.sh generates. Our fixtures need
\* no constants, so this one declares none -- it exists to prove that an
\* empty-but-legal fragment is accepted rather than refused along with the
\* three below.
