\* A directive riding along at the END of a CONSTANT line, where an anchored
\* `^INVARIANT` match cannot see it. TLC's .cfg parser is token-oriented, not
\* line-oriented: measured, `CONSTANT Unused = 6 INVARIANT Bad` checks Bad.
CONSTANT Limit = 6 INVARIANT Probe
