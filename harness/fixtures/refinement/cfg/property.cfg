\* Row 2 of the disjointness matrix: the refinement PROPERTY, checked alone.
\* Gate!RefinementConfigured is satisfied (rc=0) whether the mapping is the
\* correct one or the frozen one -- which is exactly the half TLAiBench has
\* and exactly why it is not enough.
SPECIFICATION Spec
PROPERTY Refines
