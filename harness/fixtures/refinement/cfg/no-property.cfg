\* Row 1 of the disjointness matrix: the .cfg that never declared the
\* refinement PROPERTY at all. Gate!RefinementConfigured fires (rc=10); the
\* probe is not in play, so it says nothing.
SPECIFICATION Spec
