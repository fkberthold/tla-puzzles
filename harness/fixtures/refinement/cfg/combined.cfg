\* The .cfg exactly as V2-PLAN.md section 5.4 draws it -- all three lines in
\* one run -- kept as a fixture because measuring it is what showed the
\* harness must NOT do this.
\*
\* Measured on TLC 2026.03.04.183147 against correct/Concrete.tla: rc=12,
\* "4 states generated, 4 distinct states found", search depth 4. The
\* invariant violation stops the run. The full reachable space is 7 states,
\* and the temporal property was never evaluated over the 3 that were never
\* generated.
\*
\* So a combined run reports only that the probe fired. It cannot certify
\* the refinement, because a violated invariant preempts the property check
\* and truncates the state space under it. refinement.sh runs the two
\* channels as two separate TLC invocations for this reason.
SPECIFICATION Spec
PROPERTY Refines
INVARIANT Probe
