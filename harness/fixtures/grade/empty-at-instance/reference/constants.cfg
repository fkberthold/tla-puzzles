\* THE COLLAPSED INSTANCE, and the whole fixture.
\*
\* An author shrinking a problem to keep the state space small picks the
\* smallest roster they can, and this is the bottom of that slope. With no
\* signers named, QuorumRefObl!Req_unanimity quantifies over an empty set and
\* is true of every observation there is.
\*
\* Nothing else in the package changes. `reference-three-signers/` holds the
\* same two modules under a roster of three, where the same obligation picks
\* out the submissions that issue too early.
CONSTANT Signers = {}
