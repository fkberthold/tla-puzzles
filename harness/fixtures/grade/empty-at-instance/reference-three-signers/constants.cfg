\* THE SAME PACKAGE AT AN INSTANCE THAT CAN GRADE.
\*
\* The two modules beside this file are byte for byte the ones in
\* `reference/`. Only the roster moves, and with three signers named
\* QuorumRefObl!Req_unanimity refuses every observation that issues a
\* document early.
\*
\* This half of the pair is what makes a refusal at the collapsed instance a
\* fact about the instance rather than about the package. It also has to keep
\* grading, so a gate that refuses both has gone too far.
CONSTANT Signers = {"a", "b", "c"}
