\* Standalone TLC config for the abstract spec.
\* The abstract spec is also instantiated by DistributedCounter via INSTANCE WITH;
\* this cfg lets you run it in isolation to see its behavior shape.
CONSTANT N = 3

SPECIFICATION Spec

INVARIANT TypeOK
PROPERTY EventuallyDone
