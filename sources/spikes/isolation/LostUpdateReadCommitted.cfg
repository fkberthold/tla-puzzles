\* One row, two clients. Value only has to reach the number of clients.
CONSTANTS
  Key   = {"counter"}
  Txn   = {"t1", "t2"}
  Value = {0, 1, 2}

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoLostUpdate

\* Every transaction reaching "done" is a terminal state on purpose.
CHECK_DEADLOCK FALSE
