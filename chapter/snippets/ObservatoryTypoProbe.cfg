\* THE PROBE, on the frozen mapping.
\* Run: ./run.sh Observatory.tla ObservatoryTypoProbe.cfg
\* Expected: rc=0, "No error has been found."  Here rc=0 is the failure: the
\* mapped abstract state never left its initial value, so RefinesTypo passed
\* on the stuttering disjunct alone and checked nothing at all.
CONSTANTS
    Proposals = {p1, p2}
    Dishes    = {d1, d2}
    Needed    = 2
    Idle      = Idle

SPECIFICATION
    Spec

INVARIANT
    FrozenTypo

CHECK_DEADLOCK
    FALSE
