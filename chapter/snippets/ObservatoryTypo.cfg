\* THE TRAP.  One character wrong -- `>` where `>=` was meant -- and the
\* mapping is frozen.  TLC checks the same 46 states and reports success.
\* Run: ./run.sh Observatory.tla ObservatoryTypo.cfg
\* Expected: rc=0.  That is the problem.
CONSTANTS
    Proposals = {p1, p2}
    Dishes    = {d1, d2}
    Needed    = 2
    Idle      = Idle

SPECIFICATION
    Spec

PROPERTY
    RefinesTypo

CHECK_DEADLOCK
    FALSE
