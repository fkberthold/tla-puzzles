\* The rung below full refinement: check only that every concrete step is an
\* abstract step (or invisible).  No Init, no liveness, nothing else.
\* Run: ./run.sh Observatory.tla ObservatoryIProp.cfg
\* Expected: rc=0.
CONSTANTS
    Proposals = {p1, p2}
    Dishes    = {d1, d2}
    Needed    = 2
    Idle      = Idle

SPECIFICATION
    Spec

PROPERTY
    IProp

CHECK_DEADLOCK
    FALSE
