\* The frozen-mapping probe, on the all-ones mapping.
\* Run: ./run.sh RunningH.tla RunningHAllOnesProbe.cfg
\* Expected: rc=12.  By the probe's rule that is a pass -- the mapping moves.
\* It is still the wrong mapping.  The probe is a floor, not a ceiling.
CONSTANTS
    Vals      = {1, 2}
    MaxInputs = 3

SPECIFICATION
    Spec

INVARIANT
    NotFrozenAllOnes

CHECK_DEADLOCK
    FALSE
