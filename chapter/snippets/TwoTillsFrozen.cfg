\* The hole, in miniature.  `takings <- 0` never changes, so every step of
\* TwoTills satisfies the UNCHANGED disjunct and `Ring` is never evaluated.
\* Run: ./run.sh TwoTills.tla TwoTillsFrozen.cfg
\* Expected: rc=0.
CONSTANT
    Max = 3

SPECIFICATION
    Spec

PROPERTY
    RefinesFrozen

CHECK_DEADLOCK
    FALSE
