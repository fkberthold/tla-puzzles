\* A mapping that computes, moves, tracks nothing, and passes anyway.
\* Run: ./run.sh RunningH.tla RunningHAllOnes.cfg
\* Expected: rc=0 on the refinement.
\* Run the probe on it too (RunningHAllOnesProbe.cfg) and the probe is happy:
\* rc=12.  Both checks pass and the mapping is still worthless.
CONSTANTS
    Vals      = {1, 2}
    MaxInputs = 3

SPECIFICATION
    Spec

PROPERTY
    RefinesAllOnes

ALIAS
    Alias

CHECK_DEADLOCK
    FALSE
