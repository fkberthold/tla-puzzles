\* THE PROBE, on the mapping we mean.
\* Run: ./run.sh Observatory.tla ObservatoryProbe.cfg
\* Expected: rc=12, "Invariant Frozen is violated."  The violation is the
\* pass: it proves the mapped abstract state moves, so the refinement check
\* above it had something to evaluate.
CONSTANTS
    Proposals = {p1, p2}
    Dishes    = {d1, d2}
    Needed    = 2
    Idle      = Idle

SPECIFICATION
    Spec

INVARIANT
    Frozen

CHECK_DEADLOCK
    FALSE
