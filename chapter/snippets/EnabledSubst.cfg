\* Run: ./run.sh EnabledSubst.tla
\* Expected: rc=0.  `Different` holds in every state, which is the point:
\* substituting into ENABLED is not the same as substituting into the action
\* underneath it.
SPECIFICATION
    Spec

INVARIANT
    Different

CHECK_DEADLOCK
    FALSE
