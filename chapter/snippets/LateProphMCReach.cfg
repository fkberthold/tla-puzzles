\* And yet Init and Next, on their own, reach a state where p = Never.
\* Run: ./run.sh LateProphMC.tla LateProphMCReach.cfg
\* Expected: rc=12.
\*
\* Put the two runs side by side: the state machine allows p = Never, the
\* full spec does not.  The liveness conjunct changed what states are
\* possible.  That is what "not machine closed" means, and it is also why
\* TLC cannot show it to you in one run -- TLC builds its state graph from
\* Init and Next and ignores the fairness conjunct while doing so.
CONSTANTS
    Vals    = {a, b}
    Nothing = Nothing
    Never   = Never

SPECIFICATION
    Safety

INVARIANT
    NeverNotPicked

CHECK_DEADLOCK
    FALSE
